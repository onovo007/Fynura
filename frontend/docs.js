document.querySelector('#metrics').insertAdjacentHTML('afterend','<section id="early-signals" data-title="Early Signal Detection CUSUM baseline thresholds seasonality"><h2>Changes in reported cases</h2><p>Use the country selector to explore monthly measles reports and compare the latest available year with an earlier seasonal pattern. This is a historical screening tool, not a forecast or an official outbreak alert.</p><h3>How the comparison works</h3><p>The latest 12 reported months are compared with the preceding 60 months. Each calendar month has its own expected count, based on the same month in those five baseline years. Variability is estimated from the baseline after accounting for monthly differences. Monitoring months are not used to fit the baseline.</p><p>The one-sided CUSUM accumulates standardized increases: S = max(0, previous S + (observed − expected) / baseline variation − 0.5). A comparison threshold of 5 highlights a sustained increase. These settings have not been validated for operational outbreak alerts.</p><h3>Which data can be used?</h3><p>Only uninterrupted monthly country reports with source references are used. Missing months are not filled with zero. The archive reflects the latest available source version; revisions, testing changes and disruptions such as the pandemic can affect comparisons. Check reporting dates before interpreting a signal as recent activity.</p><p>Cholera annual history and separate Ebola outbreak totals are available in Historical evidence. They are not treated as monthly surveillance sequences.</p><p><a href="https://www.itl.nist.gov/div898/handbook/pmc/section3/pmc323.htm" target="_blank" rel="noopener">About CUSUM comparisons</a></p></section>');
document.querySelector('.docs-side').insertAdjacentHTML('beforeend','<a href="#early-signals">Early signal detection</a>');
document.querySelector('#limitations p').textContent='Coverage and reporting dates vary by source. Missing reports do not mean there are no cases. Historical comparisons help identify patterns but do not establish that a new outbreak has begun.';
document.querySelector('#global-intelligence p').textContent='Map letters and colors identify verified threats; segmented markers indicate multiple threats in one country. Marker size does not represent incidence or case magnitude. No marker does not establish zero activity.';
document.querySelector('#metrics').insertAdjacentHTML('beforeend','<h3>Infection fatality ratio (IFR)</h3><p>IFR requires total infections, including unreported infections. It cannot be inferred from reported cases. Current surveillance evidence does not support IFR estimation.</p>');
document.querySelector('#responsible-use').insertAdjacentHTML('beforeend','<p>Supported scope includes public health, epidemiology, biostatistics, surveillance, population health and scientific evidence interpretation, including legitimate sexual and reproductive health education. Fynura must not facilitate deliberate transmission, pathogen weaponization, concealment of disease, surveillance fraud or targeting vulnerable populations. It does not diagnose individuals or prescribe personal treatment. Methods explanations are distinct from source-grounded surveillance findings.</p>');
const sections=[...document.querySelectorAll('[data-title]')],input=document.querySelector('#docs-search'),results=document.querySelector('#search-results');function search(){const q=input.value.trim().toLowerCase();if(!q){results.hidden=true;return}const found=sections.map(s=>({s,score:(s.dataset.title+' '+s.innerText).toLowerCase().split(q).length-1})).filter(x=>x.score).sort((a,b)=>b.score-a.score).slice(0,6);results.innerHTML=found.length?found.map(x=>`<a href="#${x.s.id}"><b>${x.s.querySelector('h2').textContent}</b><br><small>${x.s.querySelector('p')?.textContent.slice(0,110)||''}…</small></a>`).join(''):'<p>No matching documentation. Try a threat, feature, or metric.</p>';results.hidden=false}input.addEventListener('input',search);results.addEventListener('click',()=>{results.hidden=true});fetch('/api/events',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({anonymous_or_user_id:localStorage.fynuraUser||crypto.randomUUID(),event_type:'docs_viewed',feature:'docs',session_id:sessionStorage.fynuraSession||(sessionStorage.fynuraSession=crypto.randomUUID())})}).catch(()=>{});
document.querySelector('#confidence').insertAdjacentHTML('beforeend','<h3>How the score is calculated</h3><p>Fynura Evidence Confidence v1 is deterministic and describes evidence support, not the probability that an answer is correct. It combines source authority, independence, agreement, recency, reporting completeness, definition consistency, temporal and geographic consistency, provenance completeness, and verification status. Open “Why this confidence?” on an answer to inspect the component scores and limitations.</p>');document.querySelector('#ask-fynura').insertAdjacentHTML('beforeend','<h3>Context envelopes</h3><p>Every contextual request carries its selected threat, disease or dataset, geography, reporting cutoff, visual identity, and supporting evidence IDs. Context is shown separately from the composer and is not inferred from a previous answer.</p>');document.querySelector('#changelog p').insertAdjacentHTML('beforebegin','<p><b>Build 0.4 · 29 Aug 2026</b> · Added explicit context envelopes, isolated cross-threat questions, structured intelligence answers, an evidence-sensitive confidence model, source citation components, and a larger responsive copilot workspace.</p>');
// Keep sign-in guidance searchable and available before the quickstart.
document.querySelector('#quickstart').insertAdjacentHTML('beforebegin', '<section id="sign-in" data-title="Sign in Google Gmail account login reviewers get started"><h2>Sign in to Fynura</h2><p>A Gmail account is enough to get started. Other Google accounts can also be used. You do not need an invitation, a Fynura password, or membership in our organization.</p><ol><li>Open the <a href="/welcome">Fynura welcome page</a> and select <strong>Continue with Google</strong>.</li><li>Choose your Google account and complete any Google security or consent prompts.</li><li>Confirm your country or location, optionally select your stakeholder role, and acknowledge the Privacy Notice and Responsible Use statement.</li><li>Select <strong>Enter Fynura</strong> to open the intelligence workspace.</li></ol><p>Use <strong>Sign out</strong> when finished. You can return to the welcome page and sign in again with the same Google account. Public sign-in does not grant access to the owner-only admin console.</p><h3>If sign-in is blocked</h3><p>Allow the Google sign-in popup for this site and retry. Work or school Google accounts may be restricted by their administrator; use a personal Gmail account if your organization permits it, or contact your administrator. Never share your Google password with Fynura support or enter it anywhere other than Google’s sign-in page.</p></section>');
document.querySelector('.docs-side a[href="#quickstart"]').insertAdjacentHTML('beforebegin', '<a href="#sign-in">Google sign-in</a>');
document.querySelector('#ask-fynura').insertAdjacentHTML('beforeend','<h3>Generate report briefs and infographics</h3><p>Select a threat, country or historical dataset, then ask “Create a report brief about this evidence” or “Create an infographic with totals, dates and sources.” You can also use the Create report brief and Create infographic buttons beside Ask Fynura. Add a specific disease, country and year range to your question when needed.</p><p>Supported requests produce a sourced preview with downloads for an editable Markdown report and SVG or PNG infographic. Review the source links and limitations before sharing. Figures are deterministic evidence summaries, not official health-authority publications or newly generated surveillance facts.</p><p>WHO-derived OWID annual records help explain historical scale. Historical briefs can show a separate latest stored country observation alongside the archive. Annual, monthly and cumulative reports are not automatically added: overlap, reporting gaps, different definitions and different outbreaks can make a combined inception-to-present total invalid.</p>');
sections.push(document.querySelector('#sign-in'));
document.querySelector('#ask-fynura').insertAdjacentHTML('afterbegin','<h3>Live research with Gemini</h3><p>Ask Fynura now uses Gemini 3.7 Flash through Google ADK and Vertex AI, with on-demand Google Search grounding and a second source-review pass. General questions are not restricted to the three dashboard diseases. Research prioritizes original health-authority reports, checks key figures and dates, and distinguishes news context from verified surveillance.</p><p>For a chart-specific question, select <strong>Include the selected visual in my question</strong> or use a question button on the visual. Otherwise, the selected chart does not define your question. Up to eight recent question/answer pairs remain in page memory for follow-ups and are sent with your request. Reload to clear them. Do not enter patient identifiers.</p><p>You will see research progress before the answer. Responses include inline source links, a source list, and an evidence-confidence and coverage panel. Search grounding is not a guarantee of correctness; no percentage is assigned to an entire web-research answer. The deterministic confidence scores described elsewhere apply to structured surveillance evidence. Live research does not automatically update dashboard figures.</p><h3>Use the microphone</h3><p>Select <strong>Dictate</strong>, allow Chrome microphone access, speak, then review the transcript and select <strong>Send</strong>. Dictation does not submit automatically. If blocked, check the site controls beside Chrome’s address bar and Windows microphone privacy settings. The browser’s speech service may process audio; Fynura does not store raw audio. Typed questions remain available.</p>');
// Current behavior supersedes the earlier release notes, which remain historical.
document.querySelector('#ask-fynura > p').textContent='Ask about public health and scientific methods, or inspect the evidence behind a monitored outbreak. Scientific explanations and source-reported surveillance are clearly distinguished.';
document.querySelector('#ask-fynura').insertAdjacentHTML('beforeend','<h3>Scientific knowledge and capacity building</h3><p>Ask about epidemiology, biostatistics, Bayesian methods, implementation science, spatial analysis, health data science, research methods or risk communication. For example: “What is A/B test and how does it apply in epidemiology?” Then ask for a practical example, assumptions or limitations. A new topic overrides an unrelated chart.</p><p>Source-linked scientific answers show qualitative evidence support, not a surveillance-confidence percentage. When live verification is unavailable, a safe methods question may receive a clearly labeled general scientific explanation without citations. This fallback does not provide current outbreak counts. A provider failure is shown as an error, not disguised as an answer.</p><p>Use “Ask about this signal” to interpret the selected country’s computed CUSUM, baseline, threshold and reporting cutoff. The result is exploratory historical screening, not an operational forecast. Source-registry questions report application integration status rather than assuming every listed authority is an active feed.</p>');
document.querySelector('#faq').querySelectorAll('p').forEach(p=>{if(p.textContent.includes('Missing data is never'))p.textContent='No. Missing reports do not mean there are no cases.';});

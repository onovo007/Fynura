async function installEvidenceNetwork(){const response=await fetch('/api/sources'),registry=await response.json(),items=registry.sources;const orbit=document.querySelector('.orbit');orbit.outerHTML=`<aside class="network-hero"><small>GLOBAL EVIDENCE NETWORK</small><h2>Global evidence network</h2><strong>WHO · Africa CDC · ECDC · PAHO · CDC · National Health Authorities</strong><p>Authoritative evidence, harmonized where appropriate and traceable to source.</p><div class="network-flow"><div class="source-nodes">${items.slice(0,6).map(s=>`<button data-source="${s.source_id}" class="${s.operational?'operational':s.integration_status}"><b>${s.display_name}</b><i>${s.operational?'Operational':s.integration_status.replace('_',' ')}</i></button>`).join('')}</div><div class="network-core"><span>F</span><b>FYNURA</b><small>VERIFIED INTELLIGENCE</small></div></div><a href="#harmonization">How Fynura harmonizes evidence →</a><em>Harmonized where scientifically appropriate. Source differences remain visible.</em></aside>`;const sourceSection=document.createRange().createContextualFragment(`<section class="section source-explorer reveal visible" id="sources"><div class="heading"><div><p>05 / SOURCE EXPLORER</p><h2>Evidence has an identity.</h2></div><span>${registry.summary.source_families} authoritative source families · ${registry.summary.operational_feeds} verified operational feed</span></div><div class="source-grid">${items.map(s=>`<article><div><small>${s.scope.toUpperCase()} · ${s.region}</small><span class="source-status ${s.operational?'operational':s.integration_status}">${s.operational?'Operational':s.integration_status.replace('_',' ')}</span></div><h3>${s.display_name}</h3><p>${s.role}</p><dl><dt>Diseases</dt><dd>${s.diseases_supported.join(' · ')}</dd><dt>Resolution</dt><dd>${s.geographic_resolution.join(' · ')}</dd><dt>Retrieval</dt><dd>${s.retrieval_method.replaceAll('_',' ')}</dd></dl><button data-source="${s.source_id}">Inspect source</button></article>`).join('')}</div></section><section class="section harmonization reveal visible" id="harmonization"><div class="heading"><div><p>06 / EVIDENCE HARMONIZATION</p><h2>Multi-source does not mean sum every source.</h2></div><span>Fynura preserves source-reported values and only combines compatible, non-overlapping observations.</span></div><div class="harmonize-flow"><b>Discover</b><i>→</i><b>Normalize</b><i>→</i><b>Match</b><i>→</i><b>Compare</b><i>→</i><b>Resolve or preserve conflict</b></div><p>Canonical observations are selected deterministically using geography-specific authority, compatible definitions, reporting cutoffs, recency and provenance. Incompatible observations remain visible as non-comparable evidence.</p></section>`);document.querySelector('#trust').before(sourceSection);document.querySelectorAll('[data-source]').forEach(b=>b.onclick=()=>showSource(items.find(s=>s.source_id===b.dataset.source)));const trustLabel=document.querySelector('#trust .heading p');if(trustLabel)trustLabel.textContent='07 / TRUST & METHODOLOGY'}
function showSource(source){const panel=document.querySelector('#health');panel.hidden=false;panel.innerHTML=`<div><strong>${source.organization}</strong><button aria-label="Close">×</button></div><dl><dt>Scope</dt><dd>${source.scope} · ${source.region}</dd><dt>Role</dt><dd>${source.role}</dd><dt>Fynura status</dt><dd>${source.operational?'Operational':source.integration_status.replace('_',' ')}</dd><dt>Evidence</dt><dd>${source.diseases_supported.join(' · ')}</dd><dt>Capabilities</dt><dd>${source.indicators_supported.join(' · ')}</dd></dl>`;panel.querySelector('button').onclick=()=>panel.hidden=true}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>installEvidenceNetwork().catch(()=>{}));else installEvidenceNetwork().catch(()=>{});
